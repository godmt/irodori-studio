# Irodori Studio Design & UX Policy

Irodori Studioは、Irodori-TTSをローカルで扱うデスクトップファーストの制作ツールです。見栄えだけでなく、ユーザーが「次に何を押せばよいか」「設定がどこへ保存されたか」「別の画面でも同じ操作ができるか」を迷わないことを最優先にします。

## 1. プロダクトの設計原則

1. **同じ概念は、どのタブでも同じ見た目・同じ操作にする。** 同じボイス名をクリックしたとき、一方では設定が開き、他方では選択肢が開くような多義的な挙動を作りません。
2. **ユーザーが作ったものを失わせない。** 画面切り替え、名前変更、再読み込み、保存先の移動で、台本・録音・ボイス・モデルの紐付けを切りません。
3. **保存形式ではなく、ユーザーの作業単位を見せる。** GUIではプロジェクト、学習データセット、ボイス、学習済みモデルとして扱います。JSON、manifestなどの内部形式を操作名にしません。
4. **一覧・選択・編集を分離する。** 名前部分は選択、鉛筆は名前変更、歯車は設定、ゴミ箱は削除です。一つの領域に複数の意味を持たせません。
5. **主要作業は言葉で、補助操作はアイコンで示す。** 「録音を開始」「採用して次へ」「学習を開始」のような中心操作は短い動詞を表示します。作成・名前変更・削除・設定・開く・保存・書き出しなどのコンパクトな補助操作は、Phosphor Iconsのアイコンのみを表示し、冗長な隣接テキストを付けません。

## 2. 情報設計とタブの責務

| タブ | ユーザーの目的 | 必須の操作UI |
| --- | --- | --- |
| 台本制作 | 複数行の音声作品を作る | プロジェクトの作成・選択・名前変更・削除・保存、既定ボイス選択、文章追加、行の追加・並べ替え・複製・ボイス変更・生成・再生・削除、連続再生、書き出し |
| 配信 | 文章を低遅延で順次発話する | ボイス選択、品質選択、入力、キュー追加、停止、履歴から再生、出力デバイス、再生音量 |
| 録音 | コーパスを順番に読み、学習用発話を収録・採用する | 収録先の学習データセットの作成・選択・名前変更・削除、コーパス切り替え、マイク選択、録音・試聴・録り直し・採用、文章移動と状態絞り込み |
| 学習 | 収録音声または手持ち音声から学習データを整え、モデルを作る | 学習データセット管理、録音への移動、複数音声の選択・前処理・停止、任意の文字修正・採用・除外、モデル名、方式・詳細設定、開始・停止・再開・全再実行、履歴と学習済みモデル管理 |

ボイスライブラリはタブ固有ではなくStudio全体の機能です。全タブのヘッダーに同じ `UserSound` アイコンを置き、必ず同じボイスライブラリを開きます。台本制作と配信のボイス名部分は、どちらもボイスを選ぶだけの共通ドロップダウンです。

ヘッダー内のボイスライブラリは、すべてのタブで操作列の一番右に置きます。Studio全体へ作用する設定入口の位置をタブごとに動かしません。

ボイスライブラリの一覧はドラッグ＆ドロップで並び替えられます。ドラッグハンドルを表示して操作可能であることを示し、キーボードではハンドルにフォーカスしてSpace、上下矢印、Spaceの順でも移動できるようにします。並び順は共有ライブラリへ自動保存し、台本制作・配信の選択肢と再起動後の一覧へ同じ順序を反映します。

制作ファイルの書き出しは台本制作だけの操作です。共通ヘッダーや配信には置かず、読み上げ台本領域の右下に、常に到達できるアイコンだけのフローティング操作として配置します。

### 録音と学習の境界

- 録音タブはコーパス収録の専用ワークスペースです。長尺音声の取り込み、文字起こし確認、モデル設定を混在させません。
- 学習タブは `学習データを用意する → 必要な箇所だけ確認する（任意） → モデルを学習する` の司令塔です。コーパス録音から来た音声と、複数の手持ち音声から来た音声は、同じ学習データセットへ合流します。
- 「学習データセット」は両タブで同じ保存対象です。録音タブでは選択欄を「収録先データセット」と表現しても、別種のリソースは作りません。
- 学習データセットの選択状態は録音・学習で一つだけです。どちらのタブで変更しても他方へ即時反映し、「コーパスを録音」などの移動操作には個別の引き継ぎ処理を持たせません。
- 長尺音声の分割・文字起こし・構造的な品質判定に加え、形式統一・前後無音調整・音量正規化までを、学習データセットを作る共通加工とします。学習開始後は同じ加工を繰り返さず、モデル固有の特徴量生成と最適化だけを行います。

### 学習データの基本理念

この境界はIrodori-TTS固有の都合ではなく、音声・画像・文章などの学習データを扱う機能全体の設計原則です。

1. **RAW原本は不変に保つ。** ユーザーが選んだファイルや収録直後のデータは、再加工・監査・将来の方式変更に戻れる唯一の根拠です。変換結果で上書きせず、データセットの`raw/`へ保持します。
2. **方式に依存しない加工は、データセットへ入る時に一度だけ行う。** 形式・サンプルレート・チャンネル、明白な前後余白、共通音量など、どの学習方式にも必要な条件はデータセットの標準形へ揃えます。現在の音声標準形は48 kHz・モノラル・PCM 16-bit WAV、前後のみ-45 dBFS判定と180 ms余白、-16 LUFSです。文中の間や演技上の沈黙は維持します。
   文字起こしは自動生成・手動修正とも確定時に同じNFKC正規化を適用し、日本語入力で生じやすい全角英数・`？`・`！`などの表記揺れを学習前に統一します。原文の意味や句読点そのものは削除しません。
3. **方式固有の加工は、学習ジョブに閉じ込める。** DACVAE latent、token、embedding、augmentationなど、モデルや学習方式を変えると無効になる成果物はデータセットへ混ぜません。
4. **加工済みデータを学習時に再加工しない。** 学習ジョブは採用済み標準形のハッシュを検証して固定スナップショットを作り、そのままモデル固有処理へ渡します。二重正規化やジョブごとの音質差を作りません。
5. **自動加工は再現・監査・再開できるようにする。** pipeline version、原本と出力のハッシュ、入力条件、適用値、処理日時を各要素へ記録します。同じ版の有効な成果物は既定でスキップし、版変更・破損・明示的な全再実行時だけRAWから再生成します。

この3層を `RAW原本 → 共通の標準データセット → モデル固有の学習成果物` として保つことで、ユーザーが一度整えたデータをSpeaker Inversion、LoRA、将来の別方式で共用でき、失敗や中断でも作業を最初からやり直さずに済むようにします。

### 合成音声から作る学習データの適用範囲

Voice-Design-Clonerの調査（2026-08-15）では、単一の参照音声を声の条件としてAICAなどの文章を一括合成し、その音声・文章対からIrodori-TTSのLoRAを学習する処理を確認しました。これは実録音を変形する一般的なData Augmentationというより、音声クローンを教師にした合成コーパス生成と、別の永続モデルへの蒸留です。

- **実在話者の再現品質を上げる用途には採用しません。** 合成音声だけを学習ターゲットにしても、教師となる音声クローンが失った本人固有の情報は回復できず、発音・韻律・音響上の誤りを正解として固定する可能性があります。実在話者には、本人の許諾を得た複数の実録音を正本として使います。
- **非実在のVoice Design音声を固定する用途には意味があります。** Voice Designで偶発的に得られた単発音声そのものが正解の声である場合、その声でコーパスを合成してSpeaker InversionまたはLoRAへ落とし込むことで、参照音声なしの再利用、声質の安定、別モデルへの移植を目指せます。これは元の教師音声を超える本人再現ではなく、設計した架空音声の蒸留・永続化です。
- AI生成音声を導入する場合は実在話者の`RAW`と区別し、`synthetic`由来として教師モデルとcheckpoint、参照音声、コーパスと版、原文、seed、生成設定、生成日時、ハッシュを記録します。合成データを実録音と暗黙に混ぜません。
- 現在のStudioにはVoice Designと候補を試聴・選別するガチャ導線がないため、この合成コーパス生成は実装しません。Voice Designを正式な制作フローとして設計するときに、架空音声専用の後続工程として再評価します。

判断根拠は、[Voice-Design-Cloner](https://github.com/reinehonoka/Voice-Design-Cloner)の一括音声化・LoRA学習実装、Irodori-TTSの[Speaker Inversion／LoRA学習仕様](https://github.com/Aratako/Irodori-TTS#training)、合成教師データで対象TTSを改善できる条件を示した[TTS-by-TTS](https://arxiv.org/abs/2010.13421)、合成音声を学習ターゲットにする品質上限を扱った[MimicLM](https://aclanthology.org/2026.findings-acl.1020/)です。

## 3. リソース管理の共通パターン

名前を持つリソースは、可能な限り次の順序で扱います。

1. 選択ドロップダウンまたは一覧
2. `Plus` — 作成
3. `PencilSimple` — 名前変更
4. `Trash` — 削除

アイコンボタンには表示テキストを付けません。必ず `aria-label` と `title` を設定し、36 CSS px以上の操作領域を確保します。録音や再生など、誤操作の影響が大きい主要ボタンは44 CSS px以上を基本とします。

### 名前変更

- すべて共通の名前変更ダイアログを使います。
- 現在名を入力欄へ事前入力し、開いた直後に編集できます。
- 空欄、変更前と同一、同種リソースとの重複では確定できません。
- Enterで確定、Escape・背景・閉じる・キャンセルで破棄します。
- 表示名を変えても、安定ID、録音、生成音声、学習元との紐付けは維持します。
- 名前変更の結果として保存先名も変わる場合は、ダイアログ内でその影響を短く説明します。

### 削除

- ブラウザー標準の `confirm` は使わず、共通の確認ダイアログを使います。
- 削除されるデータと維持されるデータを明記します。
- 使用中の学習済みモデルなど、別リソースを壊す削除はサーバー側でも拒否します。
- 削除ボタンは危険色を常時強調せず、ホバー時と確認時に危険性を示します。

### 保存と状態

- ボイス設定と録音は自動保存です。保存中・保存済み・失敗を状態として示します。
- 台本プロジェクトは明示保存に加えて、プロジェクト切り替え前に必ず現在内容を保存します。
- 名前変更は新規作成として扱わず、元の保存対象を移動・更新します。
- 学習データセットと学習済みモデルは内部で安定IDを維持します。録音フォルダは人が識別できるデータセット名を使用し、衝突時だけ安全な接尾辞を付けます。
- 長尺音声の取り込みは「前処理」を主要操作とし、完了後はそのまま学習へ進めます。区間ごとの文字修正・採用・除外は任意の確認導線として提供し、全件確認を完了条件にしません。自動判定で学習ペアとして成立する区間は既定で採用し、短すぎる音声や空文字などだけを確認対象へ集めます。
- 長尺音声とマイク録音の原本は`raw/`に保持し、データセットの学習用音声は同じ共通加工済みWAVへ統一します。FLACはジョブ内の一時成果物であり、WAVと管理情報の保存成功後に残しません。同じ版の有効な成果物は既定でスキップし、全上書きは明示的な再実行操作としてのみ提供します。
- 前処理履歴は学習データセット単位で管理します。失敗・中断・停止した処理は保存済み候補を再利用して再開でき、履歴削除はジョブのログと一時成果物だけを対象にして、確定済みWAVとRAW原本を残します。
- 学習の停止は破棄ではありません。停止・異常終了後は同じジョブから再開でき、通常再開は前処理済み音声、latent、チェックポイントを再利用します。「すべてやり直す」は別の明示操作とし、原本や学習データセットを削除しません。
- 長時間処理の「停止」は表示上の状態変更だけで終わらせません。学習と長尺音声前処理は子プロセスを含む実行単位全体の終了を待ち、GPU／VRAMなどの計算資源を解放します。Speaker InversionとLoRAで停止経路を分岐させません。

## 4. 操作語彙

- **作成**: 新しい空の作業単位を作る。
- **追加**: 現在の作業単位へ要素を加える。
- **開く**: 保存済みの作業単位へ切り替える。
- **選択**: 現在使う候補を切り替える。設定画面は開かない。
- **名前を変更**: 内容とIDを保ったまま表示名を変える。
- **削除**: 保存対象そのものを消す。必ず影響を確認する。
- **保存**: 現在の内容をStudioへ確定する。
- **書き出し**: Studioの外で使う制作物を生成する。

内部実装名や英語のストレージ用語を、日本語の主要操作として露出しません。

## 5. ビジュアル言語

### 色

- `--paper` / `--paper-2`: 編集面、カード、モーダル
- `--canvas`: 作業領域の背景
- `--accent`: 選択、主要操作、進行
- `--success`: 保存済み、生成済み、採用済み
- `--danger`: 削除、停止、録音中の危険状態
- `--muted`: 補足。日本語本文で低コントラストになりすぎないこと

暖色のアイボリーとオレンジを基本にし、色だけで状態を伝えません。アイコン、文言、輪郭、選択状態を併用します。

### タイポグラフィ

日本語UIは次のCSSサイズを下限とします。

- 本文・説明文: 16 px以上、複数行は `line-height: 1.5` 以上
- 制約のある操作ラベル・補助情報: 14 px
- ワークスペース見出し: 28 px
- セクション見出し: 24 px
- 小見出し: 18 px
- 12 pxを許可するもの: 装飾的な英語eyebrow、時刻、単位、ID、純粋なメタデータ

日本語を12 px以下に縮めて情報を詰め込みません。狭い場合は省略、折り返し、別行化、スクロールを使います。

### 形と密度

- 標準入力・ボタン: 高さ40–46 px、角丸9–11 px
- アイコンボタン: 36 px、円形
- 主要CTA: 高さ44–58 px
- カード: 角丸11–18 px、淡い境界線
- モーダル: 共通ヘッダー、閉じるアイコン、スクロール可能な本文

アイコンはPhosphor Iconsのみを使い、絵文字、CSSで作った代替アイコン、独自SVGを混ぜません。

### 並び替えとモーション

読み上げ台本とボイスライブラリは、同じ共通Sortable Listを使用します。

- ドラッグ開始後、元要素は完全に非表示にして寸法だけを維持し、リスト内に空き位置を作ります。半透明のゴーストを残しません。
- ポインターには、元カードと同じ寸法・内容を持つ不透明なDrag Overlayを追従させます。
- 移動候補になった周囲の要素は、挿入位置を空ける方向へ約220msのイージングで移動します。
- ドロップは約190msで着地させ、状態更新と二重に跳ねる動きを発生させません。
- ドラッグはハンドルだけから開始し、本文編集、ボイス選択、再生などのカード内操作と競合させません。
- キーボードではSpaceで掴み、上下矢印で移動し、Spaceで確定、Escapeで取り消します。モーダル内のドラッグ中にEscapeを押した場合は、モーダルを閉じずドラッグだけを取り消します。
- `prefers-reduced-motion` では移動時間を実質無効化します。

## 6. フィードバックとエラー

- 成功は短いトーストで伝え、画面の選択状態も同時に更新します。
- 失敗時は入力値と現在の選択を維持し、やり直せる状態にします。
- 処理中は対象操作だけを無効化し、アプリ全体を不用意に塞ぎません。
- 空状態には「何もない」だけでなく、次に行う操作を一文で示します。
- 自動保存に失敗し、再読み込みで失われる可能性がある場合は明確に警告します。
- 長時間処理の失敗履歴は「失敗」だけで終わらせず、ユーザーが理解できる原因、次に行う対処、必要な場合だけ開く技術情報を同じ画面から確認できるようにします。
- 学習中はステップ数に加えて「残り時間／全体時間」を表示します。開始直後は計算中とし、現在の実行で観測したステップ速度に基づく目安として継続更新します。精密な完了時刻のように誤認させません。
- Speaker Inversionは、実音声による匿名比較で500 step時点の品質が十分だった結果を既定値へ反映します。設定欄では、品質をさらに求める場合の目安として1000 step以上を案内します。個別の発音ミスはモデル全体の品質不足と即断せず、同じcheckpointのリテイクで判定します。
- LoRAは単一話者のStudioデータセットを前提に1,500 stepを既定とし、高品質・追い込みは3,000 stepを目安として設定欄に示します。上流30,000-step設定のwarmup・安定・減衰比率は、選択した総stepへ自動的に縮尺します。Speaker Inversionで十分な品質が得られる場合はLoRAを必須工程にせず、必要性を比較試聴で判断します。
- CUDA学習を開始する前に、推論モデルの一時的な使用量ではなくGPUの物理総VRAMを確認します。現行構成の推奨値はSpeaker InversionがBF16/FP16で12 GB、FP32で16 GB、LoRAがBF16/FP16で24 GB、FP32で32 GBです。公称12 GBカードが11.99 GiBと報告される差は丸めて扱います。推奨値を下回る場合だけ、共有メモリへの退避による極端な低速化や開始失敗の可能性を共通確認ダイアログで伝えます。キャンセル時は学習ジョブを作らず、常駐モデルも解放しません。環境差を考慮して禁止にはせず、明示的な続行を許可します。
- Studioが生成・検証した中間データは次工程へ渡す前に件数と成果物を検証し、0件や一部欠落を成功として先へ進めません。

## 7. アクセシビリティとリフロー

- すべてのアイコンボタンに視覚外ラベルを付けます。
- 選択中のタブ、テイク、方式は意味のある状態属性で表します。
- フォーカスは輪郭で明示し、キーボードだけで主要フローを完了できるようにします。
- モーダルは `role="dialog"` と `aria-modal` を持ち、最上位のモーダルだけをEscapeで閉じます。
- 200%ズーム相当では、コントロールを別行化し、重なり、水平読書スクロール、到達不能な操作を発生させません。
- 省略したデバイス名やリソース名は、開いた選択肢またはツールチップで全文を確認できるようにします。

## 8. 禁止するパターン

- 同じ名前部分へ「選択」と「設定を開く」の両方を割り当てる
- 直接編集、専用モーダル、ブラウザーconfirmをリソースごとに混在させる
- 同じ一覧や追加操作を同一画面へ重複配置する
- タブ固有の操作を共通ヘッダーへ置き、無関係なタブでも表示する
- 明白なユーティリティアイコンの横へ「追加」「変更」「削除」などの冗長な文字を置く
- 権限取得前にStudio独自の許可説明モーダルを表示する
- 保存済み設定をページ再訪時に既定値で上書きする
- JSON、manifest、内部IDをユーザー操作の中心にする
- 日本語を14 px未満へ縮小してレイアウトを成立させる

この文書はUI変更時の判断基準です。新しいタブやリソースを追加するときは、まず既存の共通パターンで表現できるか確認し、例外を作る場合は理由と新しい規則をこの文書へ先に追記します。
